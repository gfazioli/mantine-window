import type { WindowGroupFactory } from '@gfazioli/mantine-window';
import type { StylesApiData } from '../components/styles-api.types';

export const WindowGroupStylesApi: StylesApiData<WindowGroupFactory> = {
  selectors: {
    root: 'Root element — container with position: relative',
  },

  vars: {},
};
